import math
def toSquared (number):
    # pow(numero, potencia) -> Elevar un numero a una potencia determinada.
    return
    result = pow(number,2) 
    print(result)
    

def allPow (number, raised) : 
    result = pow(number, raised)
    print(result)

def root (number):
    # math.sqrt(numero) -> Raiz cuadrada
    # math.cbrt(numero) -> Raiz cubica
    return
    result = int(math.sqrt(number))
    print(result)

